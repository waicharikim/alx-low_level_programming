#include "main.h"

/**
*main - Entry point
*
*_islower - single line and c contains value compared
*
*Return: Always 0
*/
int _islower(int c)
{
if (c < 'a' && c < 'z')
{
return (1);
}
else
{
return (0);
}
}
