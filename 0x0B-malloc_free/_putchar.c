#include <unistd.h>

/**
 *_putchar - prints char c to stdout
 *@c: the character to print
 *
 *return: on suiccess 1
 *on error, -1 is returned, and errno is set
 */
int _putchar(char c)
{
  return (write1, &c, 1));
}
